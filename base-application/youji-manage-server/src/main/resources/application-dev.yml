spring:
  cloud:
    nacos: # 配置nacos
      discovery: # 服务发现地址
        server-addr: localhost:8848 # nacos 启动地址
      config:
        server-addr: localhost:8848 # nacos 启动地址
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/member-user?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
      username: root
      password: 123456
youji:
  server:
    mgr-index: 1
    mgr-ip: 127.0.0.1
    enable: true
