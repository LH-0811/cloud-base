<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>modules</artifactId>
        <groupId>com.cloud</groupId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.cloud.modules</groupId>
    <artifactId>modules-lh-security</artifactId>
    <packaging>pom</packaging>

    <modules>
        <module>lh-security-core</module>
    </modules>


    <description>
        server
        1. token管理
            1.1 token 生成
            1.2 token 存储（token-用户信息）
                1.2.1 内存
                1.2.2 mysql
                1.2.3 redis
            1.3 token 校验
            1.4 token 续期
            1.5 token 销毁
        2. 用户认证
            2.1 凭证类管理
            2.2 凭证类验证器
                2.2.1 用户基本信息
                2.2.2 用户权限信息
            2.3 调用token管理组件生成token，并颁发
            2.4 公开用户认证接口(基于netty)
            2.5 公开用户token销毁接口
        3. 权限校验
            3.1 公开权限校验接口(基于netty)

        client
        1. 用户认证
            1.1 调用server的认证接口
        2. 权限校验
            2.1 调用server的校验接口
    </description>
</project>
